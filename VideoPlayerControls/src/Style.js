import { StyleSheet } from 'react-native';

var Style = StyleSheet.create({
    backgroundVideo: {
    position: 'absolute',
    top: 0,
    left: 0,
    height: 100,
    right: 0,
  }
});

export default Style;